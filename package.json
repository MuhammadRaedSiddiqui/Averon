{
  "name": "averon",
  "version": "1.0.0",
  "description": "Agent Forge webhook backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Tests pass\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
